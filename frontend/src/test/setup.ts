/**
 * Loaded before every test file.
 *
 * `jest-dom` adds the matchers that describe a document rather than an object —
 * `toBeInTheDocument`, `toHaveAccessibleName`, `toBeDisabled`. Without them a component test
 * asserts on React internals, which is the kind of test that passes while the screen is broken.
 */
import "@testing-library/jest-dom/vitest";
